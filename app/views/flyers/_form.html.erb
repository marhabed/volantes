<%= form_for(@flyer) do |f| %>
  <% if @flyer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flyer.errors.count, "error") %> prohibited this flyer from being saved:</h2>

      <ul>
      <% @flyer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="field">

		<%= f.fields_for :branch_flyers do |builder| %>    
	    	<%= builder.hidden_field  :branch_id, value: @branch_id %>
	    	<%= builder.hidden_field :flyer_id %>
	  	<% end %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :file_url %><br />
    <%= f.text_field :file_url %>
  </div>
  <div class="field">
    <%= f.label :active %><br />
    <%= f.check_box :active %>
  </div>
    <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
